node_modules/
dist/
build/
*.log
.env
*.local

# 秘書子の絵の原本（生成したままのもの）は、プロジェクトの外に置いてある。
#   Documents/Claudecode/秘書子_原本/
# アプリが使うのは assets/ に入れた加工済みのほう（縮小・ロゴ除去ずみ）。
# 万一ここに置かれても、うっかり公開リポジトリへ入らないように残しておく。
秘書子/
